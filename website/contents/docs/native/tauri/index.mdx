---
title: Tauri
description: Tauri is a framework for building tiny, fast desktop apps using web technologies and Rust. It allows you to create cross-platform applications with secure Rust-powered backends and modern frontend frameworks.
---

## Structure

<Files
  items={[
    {
      type: "folder",
      name: "src-tauri",
      children: [
        { type: "file", name: "main.rs" },
        { type: "file", name: "tauri.conf.json" },
      ],
    },
    {
      type: "folder",
      name: "src",
      children: [
        { type: "file", name: "main.ts" },
        { type: "file", name: "App.tsx" },
        {
          type: "folder",
          name: "components",
          children: [{ type: "file", name: "Header.tsx" }],
        },
      ],
    },
    { type: "file", name: "package.json" },
    { type: "file", name: "tsconfig.json" },
  ]}
/>

### `src-tauri/`
Rust backend configuration and entry point.

```rs
fn main() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

### `src/`
Frontend source code.

### `components/`
Reusable UI components.

### ✅ Tauri API Example

```ts
import { invoke } from '@tauri-apps/api/tauri';

async function greet(name: string) {
  const response = await invoke('greet', { name });
  console.log(response);
}
```

### 🛠️ Development

```bash
npm run tauri dev
```
